/**
 * Number of numbered build steps in the manual. Keep in step with manualPages.
 *
 * 13 since the leg build was split to run one side at a time: the long legs,
 * the spindle nut and the leg joints are each their own step per side.
 */
export const MANUAL_LEAVES_COUNT = 13;
